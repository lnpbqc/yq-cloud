spring:
  profiles:
    include: route # application-route.yaml + application.yaml ??
    # active: dev # application-dev.yaml test application-test.yaml ??
  application:
    name: shop-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
server:
  port: 80