server:
  port: 8091
  tomcat:
    threads:
      max: 10
#  tomcat:
#    threads:
#      max: 200
#      min-spare: 10
spring:
  application:
    name: shop-order-server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shop-order?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf-8
    username: root
    password: lnpbqc
#  zipkin:
#    base-url: http://127.0.0.1:9411/ #zipkin server的请求地址
#    discoveryClientEnabled: false #让nacos把它当成一个URL，而不要当做服务名，不用注册到nacos上 （新版本springcloud不用配了）
#  sleuth:
#    sampler:
#      probability: 1.0 #采样的百分比
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        port: 8719
        dashboard: localhost:8858
      web-context-unify: false

product-service:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule
logging:
  level:
    com.gec.shoporderserver.api: DEBUG  # 前面是包名 : 输出级别